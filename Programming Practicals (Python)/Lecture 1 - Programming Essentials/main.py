# ----------------------------------------------------------------------------------------------------------------------

# Calculating average from three inputs.
number_1 = float(input("Enter the first number: "))
number_2 = float(input("Enter the second number: "))
number_3 = float(input("Enter the third number: "))

average = (number_1 + number_2 + number_3) / 3

print("The average is: ", average)

# ----------------------------------------------------------------------------------------------------------------------

# Identifying Statements, Assignment statements, Variables, Numeric values, String values,
# Arithmetic expressions, Built-in functions.

# number_1, number_2, number_3, average = Variables
# float, input, print = Built-in function
# "Enter the first number: " = String value
# (number_1 + number_2 + number_3) / 3 = Arithmetic expression
# number_1, number_2, number_3, average = Numeric values
# "average =, number_1 =, number_2 =, number_3 =" = Assignment statements
# Statement is any complete line (Excluding comments)

number_1 = float(input("Enter the first number: "))
number_2 = float(input("Enter the second number: "))
number_3 = float(input("Enter the third number: "))

average = (number_1 + number_2 + number_3) / 3

print("The average is: ", average)

# ----------------------------------------------------------------------------------------------------------------------